These are programs and datasets used for numerical experiment.

benchmark.mat

Dataset are stored in data structure for n vertex (n=10,20,...,100)

optR,optT: the optimal variables.
const: constant value if the objective function is changed.
optRMSD: minimum value of RMSD

---
result.mat

Results (correct/not and computation time)
